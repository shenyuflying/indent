#include "a/b/c.h"
if(a==b && b!= c)
{
	/* some blank line follow */      
	for(i=1; i<100;i++)
	{
		func(a,b, c);
		func(d,e, f);
		func(a, b, c);
	}

	a++;
	a+b;
	a+ b;
	a +b;
	a+=1;

	a--;
	a-b;
	a-=1;

	a=*p;
	a*b;
	a/b;
	a<<1;
	a>>2;
	a>=1;
	a<=2;

	a+b+c+d+e+f;	
	a==b==c==d;
	
}
